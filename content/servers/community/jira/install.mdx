# Install — Jira MCP

<InstallSnippets
  clients={['claude-desktop', 'cursor', 'vscode', 'generic']}
  entry={{ slug: 'jira', name: 'Jira MCP', repo_url: 'https://github.com/vmware/vmcphub-placeholder-jira' }}
/>

Set `JIRA_BASE_URL` and `JIRA_API_TOKEN` in your runtime env.
